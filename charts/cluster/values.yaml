# Single source of truth for the cluster DNS zone.
# Ansible (k3s_setup) and every app chart read global.baseDomain from here.
global:
  baseDomain: sit.lyceum.usu.ru
  internalCA:
    enabled: true
    configMapName: sesc-internal-ca
    mountPath: /etc/ssl/certs/sesc-internal-ca.crt
