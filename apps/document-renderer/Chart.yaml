apiVersion: v2
name: document-renderer
description: Document renderer taskiq worker (consumes RabbitMQ, renders PDF, uploads to MinIO).
type: application
version: 0.1.0
appVersion: "0.1.0"
maintainers:
  - name: SESC-IT
