version: 1
metadata:
  name: "OpenFGA Group only policy"
entries:
  - identifiers:
      name: "policy-check-openfga-group"
    model: authentik_policies_expression.expressionpolicy
    attrs:
      expression: |
        return ak_is_group_member(request.user, name="openfga-allowed")
