apiVersion: v2
name: registry
description: Private Docker registry on k3s, exposed through the cluster Service and Ingress.
type: application
version: 0.1.0
appVersion: "3"
maintainers:
  - name: SESC-IT
dependencies:
  - name: cluster
    version: 1.0.0
    repository: "file://../../charts/cluster"
